import React from 'react';

export function AppRouter() {
  return (
    <div />
    // <Switch>
    //   {routes.map(route =>
    //     <Route path={route.path} c={route.component} />)}
    // </Switch>
  );
}
